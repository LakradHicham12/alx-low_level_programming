# inclure  " main.h "

/* *
* _isdigit - écrivez une fonction qui vérifie un chiffre (0 à 9).
* @c : caractère à vérifier
* Retour : 0 ou 1
* */
entier  _isdigit ( entier c)
{
	si (c >= ' 0 ' && c <= ' 9 ' )
		retour ( 1 );
	autre
		retour ( 0 );
}

